#Data science
import numpy as np
temperature = np.array ([32.5, 31.8, 33.0, 35.2, 36.6])
average = np.mean(temperature)
print(average)

# total = 0
# for tem in temperature:
#           total += t
# average = total/len(temperature)
